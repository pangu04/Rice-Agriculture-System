# PostgreSQL env example for docker-compose
POSTGRES_USER=postgres
POSTGRES_PASSWORD=sercret
POSTGRES_DB=rice_db
POSTGRES_PORT=5432
POSTGRES_HOST=postgres
# Do not commit real secrets. Copy this file to .env and set secure values.
